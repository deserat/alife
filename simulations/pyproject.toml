[project]
name = "alife-simulations"
version = "0.1.0"
description = "Artificial life simulator research — building block simulations"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10",
    "numpy>=2.0",
]

[tool.uv]
python-preference = "managed"
